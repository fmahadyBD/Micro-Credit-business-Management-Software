/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface RejectWithdrawal$Params {
  requestId: number;
  processedBy: string;
  reason: string;
}

export function rejectWithdrawal(http: HttpClient, rootUrl: string, params: RejectWithdrawal$Params, context?: HttpContext): Observable<StrictHttpResponse<{
}>> {
  const rb = new RequestBuilder(rootUrl, rejectWithdrawal.PATH, 'post');
  if (params) {
    rb.path('requestId', params.requestId, {});
    rb.query('processedBy', params.processedBy, {});
    rb.query('reason', params.reason, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      }>;
    })
  );
}

rejectWithdrawal.PATH = '/api/withdrawals/{requestId}/reject';
