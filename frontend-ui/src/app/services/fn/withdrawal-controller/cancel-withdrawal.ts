/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface CancelWithdrawal$Params {
  requestId: number;
  reason?: string;
}

export function cancelWithdrawal(http: HttpClient, rootUrl: string, params: CancelWithdrawal$Params, context?: HttpContext): Observable<StrictHttpResponse<{
}>> {
  const rb = new RequestBuilder(rootUrl, cancelWithdrawal.PATH, 'post');
  if (params) {
    rb.path('requestId', params.requestId, {});
    rb.query('reason', params.reason, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      }>;
    })
  );
}

cancelWithdrawal.PATH = '/api/withdrawals/{requestId}/cancel';
