/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaymentSchedule } from '../../models/payment-schedule';

export interface AddPayment$Params {
  scheduleId: number;
  amount: number;
  agentId: number;
  notes?: string;
}

export function addPayment(http: HttpClient, rootUrl: string, params: AddPayment$Params, context?: HttpContext): Observable<StrictHttpResponse<PaymentSchedule>> {
  const rb = new RequestBuilder(rootUrl, addPayment.PATH, 'post');
  if (params) {
    rb.path('scheduleId', params.scheduleId, {});
    rb.query('amount', params.amount, {});
    rb.query('agentId', params.agentId, {});
    rb.query('notes', params.notes, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaymentSchedule>;
    })
  );
}

addPayment.PATH = '/api/payment-schedules/{scheduleId}/pay';
